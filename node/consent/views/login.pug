extends layout

block content
  .container-fluid
    p.lead Once you have logged in you will get access to your consent file on the blockchain and manage all of your consents.
    p If you are an administrator you will be able to manage all consent templates and generate new consents for specific users.
    br
    form(role='form', action="/login",method="post", style='max-width: 300px;')
      .form-group
        input.form-control(type='text', name="username", placeholder='Enter Username', required)
      .form-group
        input.form-control(type='password', name="password", placeholder='Password', required)
      button.btn.btn-primary(type='submit') Submit
      b &nbsp;
      a(href='/')
        button.btn.btn-danger(type="button") Cancel